name: theaterartgaley

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    user: '0:0'
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
    expose:
      - '3000'
